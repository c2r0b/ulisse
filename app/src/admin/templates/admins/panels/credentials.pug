.panel(data-title="{{ 'credentials' | translate }}")
  .in_group(label='Username')
    input(type='text', ng-model='$e.username', readonly='')
  .in_group(label='Password')
    input(type='text', ng-model='$e.password', readonly='')
